import React from 'react';
import PackageList from '../components/PackageList';

const PackagesPage = () => {
  return (
    <div>
      <PackageList />
    </div>
  );
};

export default PackagesPage;
